#include <stdio.h>
int main()
{
   char character;
   printf("enter a character: \n");
   scanf("%c", &character);
   printf("ASCII value of %c = %d \n", character,character);
   printf("ASCII value found by vyom tayal, R.no72");
   return 0;
}